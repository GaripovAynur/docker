docker inspect stupefied_turing #Больше информации о конкретном контейнере можно получить с помощью команды docker inspect
docker diff stupefied_turing # список файлов, измененных в работающем контейнере
docker logs stupefied_turing #выполнить эту команду с указанием имени контейнера, то будет выведен список всех событий, произошедших внутри заданного контейнера
docker commit cowsay test/cowsayimage # передать в команду имя контейнера («cowsay»), имя для создаваемого образа («cowsayimage»), а также имя репозитория, в котором образ будет сохранен («test»)
docker run -d -p 8000:80 nginx #открыть нужные порты для общего доступа с помощью аргументов -p или -P в команде запуска. Такая команда перенаправляет порты хоста в контейнер
